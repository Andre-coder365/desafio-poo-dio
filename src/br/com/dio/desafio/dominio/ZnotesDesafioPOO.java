package br.com.dio.desafio.dominio;

public class ZnotesDesafioPOO {
    /*
    Modificadores de acesso = public, private, protected
    usados antes dos atributos (variáveis, constantes)

    public = qualquer classe que precisar usar e modificar esses atributos vai ter acesso.
    private = só essa classe onde ele está terá acesso diretamente
    a esse atributo.
    protected = só essa classe e as classes - filhas terão acesso
    a esse atributo

    Por default, se não colocar nada na frente (antes do atributo) fica public

    É uma boa prática utilizar o modificador de acesso private.

    Para que eu possa dizer que uma classe é filha da outra, eu uso a palavrinha
    reservada "extends" (Ex.: public class Mentoria extends Conteudo {})


     */


}
